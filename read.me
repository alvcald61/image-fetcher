To run the vite-react project locally, you need to install the dependencies and start the development server:

execute the following commands in the terminal:
npm install
npm run dev

The server should  be running  on http://localhost:5173
To open the page in the browser, type http://localhost:5173 in the address bar or press the "o" in the terminal